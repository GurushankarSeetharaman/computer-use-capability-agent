# Runtime + test dependencies.
#
# Upper bounds are pinned to the current major of each package: all four
# have shipped breaking major releases, and a take-home that stops building
# months from now because a transitive resolve drifted is a bad look.
# Versions below reflect what was current at project start.

playwright>=1.62,<2      # browser control; the only thing surface/ imports
anthropic>=1.2,<2        # Claude API client for the discovery loop (1.x; 0.x has a different surface)
pydantic>=2.13,<3        # schema + validation for the artifact and result contracts (v2 API)
pytest>=8.4,<10           # tests (floor set to what runs locally today; 9.x is current)
